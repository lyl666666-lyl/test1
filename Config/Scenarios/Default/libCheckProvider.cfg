// Configuration of reaction (ignore, warn, abort) to failed checks for different outputs.
notSetReaction = {
  motionRequest = ignore;
  headMotionRequest = ignore;
  calibrationFinished = ignore;
  passTarget = ignore;
};
multipleSetReaction = {
  motionRequest = ignore;
  headMotionRequest = ignore;
  calibrationFinished = ignore;
  passTarget = ignore;
};
